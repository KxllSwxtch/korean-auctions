# 🗓️ Динамическая логика определения weekNo для SSANCAR

**Дата:** 31 января 2025  
**Статус:** ✅ Реализовано  
**Задача:** Автоматическое определение weekNo в зависимости от дня недели

## 📋 Требования

- **Понедельник и Вторник** → `weekNo = 1`
- **Четверг и Пятница** → `weekNo = 2`
- **Остальные дни** → `weekNo = 1` (по умолчанию)

## 🔧 Реализованные изменения

### 1. Новый метод `_get_current_week_number()`

```python
def _get_current_week_number(self) -> str:
    """
    Определяет номер недели аукциона в зависимости от текущего дня недели:
    - Понедельник (0) и Вторник (1) → weekNo = 1
    - Четверг (3) и Пятница (4) → weekNo = 2
    - Остальные дни → weekNo = 1 (по умолчанию)
    """
    now = datetime.now()
    weekday = now.weekday()  # 0=Понедельник, 1=Вторник, ..., 6=Воскресенье

    if weekday in [0, 1]:  # Понедельник или Вторник
        week_no = "1"
    elif weekday in [3, 4]:  # Четверг или Пятница
        week_no = "2"
    else:  # Остальные дни (Среда, Суббота, Воскресенье)
        week_no = "1"  # По умолчанию

    logger.info(f"📅 Текущий день недели: {now.strftime('%A')} ({weekday}), используем weekNo: {week_no}")
    return week_no
```

### 2. Обновлен метод `_convert_glovis_params_to_ssancar()`

```python
# Определяем weekNo: используем переданный параметр или автоматически определяем по дню недели
week_no = params.get("auction_number")
if week_no is None:
    week_no = self._get_current_week_number()
else:
    week_no = str(week_no)
```

### 3. Обновлен метод `get_auctions()`

- Динамически определяет текущую неделю
- Автоматически отмечает активную неделю как `"default": True`
- Возвращает `"current_week"` в ответе

```python
# Получаем текущую неделю динамически
current_week = self._get_current_week_number()

# Создаем список аукционов с отметкой текущей недели
auctions = []
for i in range(1, 5):  # Недели 1-4
    week_str = str(i)
    auction = {
        "number": week_str,
        "name": f"Неделя {i}",
        "status": "active",
    }
    # Отмечаем текущую неделю как default
    if week_str == current_week:
        auction["default"] = True
    auctions.append(auction)
```

## 🧪 Результаты тестирования

### Логика по дням недели:

- ✅ **Понедельник (0)** → weekNo: 1
- ✅ **Вторник (1)** → weekNo: 1
- ✅ **Среда (2)** → weekNo: 1 (по умолчанию)
- ✅ **Четверг (3)** → weekNo: 2
- ✅ **Пятница (4)** → weekNo: 2
- ✅ **Суббота (5)** → weekNo: 1 (по умолчанию)
- ✅ **Воскресенье (6)** → weekNo: 1 (по умолчанию)

### Реальные тесты:

- ✅ **Автоматическое определение:** weekNo = 1 → 15 автомобилей
- ✅ **Принудительное указание:** weekNo = 2 → 15 автомобилей
- ✅ **API get_auctions:** Корректно отмечает активную неделю

## 📊 Гибкость системы

### Автоматический режим:

```bash
# Не передаем auction_number - система автоматически определяет weekNo
GET /api/v1/glovis/cars?page=1
```

### Принудительный режим:

```bash
# Передаем auction_number - система использует указанное значение
GET /api/v1/glovis/cars?page=1&auction_number=2
```

## 🎯 Преимущества

1. **Автоматизация** - Нет необходимости вручную переключать недели
2. **Гибкость** - Можно принудительно указать нужную неделю
3. **Логичность** - Соответствует расписанию аукционов SSANCAR
4. **Надежность** - Fallback на weekNo = 1 для неопределенных дней

## 📅 Расписание работы

| День недели | weekNo | Описание         |
| ----------- | ------ | ---------------- |
| Понедельник | 1      | Аукцион недели 1 |
| Вторник     | 1      | Аукцион недели 1 |
| Среда       | 1      | По умолчанию     |
| Четверг     | 2      | Аукцион недели 2 |
| Пятница     | 2      | Аукцион недели 2 |
| Суббота     | 1      | По умолчанию     |
| Воскресенье | 1      | По умолчанию     |

## 🔄 Совместимость

- ✅ **Frontend:** Получает актуальную неделю автоматически
- ✅ **API:** Поддерживает как автоматический, так и ручной режим
- ✅ **Логи:** Подробное логирование определения weekNo
- ✅ **Fallback:** Безопасное значение по умолчанию

---

_Теперь система автоматически выбирает правильную неделю аукциона! 🗓️✨_
