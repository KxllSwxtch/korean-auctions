# FastAPI and web framework
fastapi==0.115.12
starlette==0.46.2
uvicorn==0.34.3
gunicorn==21.2.0
python-multipart==0.0.20
orjson==3.10.12

# HTTP clients and networking
httpx==0.28.1
requests==2.32.3
aiohttp==3.10.11
# Trust roots for app/core/tls.py. Floor, not an equality pin: certifi shipped
# the compromised GLOBALTRUST roots until 2024.07.04 (GHSA-248v-346w-9cwc), so
# older releases must never be resolved, but the bundle should stay current.
certifi>=2024.7.4

# Data validation and parsing
pydantic==2.11.5
pydantic-settings==2.9.1
beautifulsoup4==4.13.4
lxml==5.4.0

# Utilities and tools
fake-useragent==2.2.0
loguru==0.7.3
tenacity==9.1.2
python-dotenv==1.1.0
pytz==2024.2
filelock>=3.13

# Additional required dependencies for auction parsing
Faker==34.0.0
cryptography
cloudscraper==1.2.71

# Scheduling
apscheduler==3.10.4

# Dependencies (auto-resolved by pip)
# Core dependencies will be installed automatically
